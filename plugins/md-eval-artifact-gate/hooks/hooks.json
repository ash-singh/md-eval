{
  "description": "Check written pages with md-eval before the Artifact tool publishes them; send weak or risky pages back once per file per session",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Artifact",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --quiet --from git+https://github.com/ash-singh/md-eval@867af23641095f5cc869d9fae0783b31d0f9baa5 md-eval-artifact-hook || true",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
