{
  "description": "Score plans with md-eval before ExitPlanMode; send weak plans back once per session",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --quiet --from git+https://github.com/ash-singh/md-eval@31ae6aa84567d14aec4484e002d68f18cfdb2ddc md-eval-plan-hook || true",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
