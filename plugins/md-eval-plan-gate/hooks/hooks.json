{
  "description": "Score plans with md-eval before ExitPlanMode; send weak plans back once per session",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "uvx --quiet --from git+https://github.com/ash-singh/md-eval@v0.2.0 md-eval-plan-hook || true",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
